### Fixed

- Locked shapes and shapes in the Review workspace now show visual activation
  feedback on hover (highlight, raised z-order, skeleton wrapping bbox) while
  editing remains disabled. Previously the skeleton bbox was never visible in
  the Review workspace because activation was skipped entirely for
  non-editable shapes
